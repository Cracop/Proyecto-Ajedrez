{
    "name": "ia chess game",
    "description": "A IA model to play a chess game",
    "repository": " https://github.com/Cracop/Proyecto-Ajedrez.git",
    "keywords": ["python", "django" ],
    "env": {
      "SECRET_KEY": {
        "description": "The secret key for the Django application.",
        "generator": "secret"
      }
    },
    "environments": {
      "test": {
        "scripts": {
          "test-setup": "python manage.py collectstatic --noinput",
          "test": "python manage.py test"
        }
      }
    }
  }
  